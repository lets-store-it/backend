type: object
properties:
  name:
    type: string
    description: The name of the TV Board
  unitId:
    type: string
    format: uuid
    description: The ID of the unit that the TV Board belongs to
required:
  - name
  - unitId
