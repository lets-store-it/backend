type: object
properties:
  name:
    type: string
    example: Exotic
  subdomain:
    type: string
    example: exotic
    minLength: 1
    maxLength: 255
    pattern: ^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$
required:
  - name
  - subdomain
