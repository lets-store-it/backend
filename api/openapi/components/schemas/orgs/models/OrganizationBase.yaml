type: object
properties:
  name:
    type: string
    example: Exotic
    minLength: 1
    maxLength: 100
  subdomain:
    type: string
    example: exotic
    minLength: 1
    maxLength: 100
    pattern: ^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$
required:
  - name
  - subdomain
